# bugURL

-- How To Run --

$ apt update
$ apt install git
$ apt install bash
$ git clone https://github.com/IslamicHackersState/bugURL
$ cd bugURL
-- Termux ---
$ bash bugURL.sh
-- Linux ---
$ sudo bash bugURL.sh

= > set target 
= > enter

